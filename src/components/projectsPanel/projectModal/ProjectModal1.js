import Modal from "../../UI/modal/Modal";

import styles from "./ProjectModal1.module.css";

const ProjectModal1 = (props) => {
  return <Modal closeModal={props.closeModal}>Ginger Beano</Modal>;
};

export default ProjectModal1;
